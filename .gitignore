# root-level node_modules junction (created by setup-dev.ps1 for Turbopack CSS fix)
node_modules/
nginx/certs/

# Stub root package-lock.json — Turbopack picks this up as the monorepo root
# instead of frontend/, causing OOM crashes on Windows. Keep it out of the repo.
/package-lock.json

# frontend
frontend/node_modules
frontend/.next
frontend/.env
frontend/.env.local

# backend
backend/venv/
__pycache__/
*.pyc
.pytest_cache/
backend/.env
backend/temp_processing/
backend/saved_documents/
backend/incoming_json/

#docs
docs/_srs_extracted.txt

# claude
.claude/settings.local.json

# OS / misc
.DS_Store
Thumbs.db

frontend/nul
